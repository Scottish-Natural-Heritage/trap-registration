import {ReturnState} from './_base.js';

const renewalIntroSelectController = (_request) => {
  // Proceed to the next page.
  return ReturnState.Positive;
};

export {renewalIntroSelectController as default};
